<div class="container py-5">
    <h1 class="mb-4">Add New Product</h1>
    
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card">
                <div class="card-body">
                    <% if (locals.error) { %>
                        <div class="alert alert-danger">
                            <%= error %>
                        </div>
                    <% } %>
                    
                    <form action="/admin/products" method="POST">
                        <div class="mb-3">
                            <label for="name" class="form-label">Product Name</label>
                            <input type="text" class="form-control" id="name" name="name" required value="<%= locals.formData?.name || '' %>">
                        </div>
                        
                        <div class="mb-3">
                            <label for="price" class="form-label">Price ($)</label>
                            <input type="number" class="form-control" id="price" name="price" step="0.01" min="0" required value="<%= locals.formData?.price || '' %>">
                        </div>
                        
                        <div class="mb-3">
                            <label for="category" class="form-label">Category</label>
                            <select class="form-select" id="category" name="category" required>
                                <option value="">Select Category</option>
                                <option value="men" <%= locals.formData?.category === 'men' ? 'selected' : '' %>>Men's Clothing</option>
                                <option value="women" <%= locals.formData?.category === 'women' ? 'selected' : '' %>>Women's Clothing</option>
                                <option value="accessories" <%= locals.formData?.category === 'accessories' ? 'selected' : '' %>>Accessories</option>
                                <option value="sale" <%= locals.formData?.category === 'sale' ? 'selected' : '' %>>Sale Items</option>
                            </select>
                        </div>
                        
                        <div class="mb-3">
                            <label for="description" class="form-label">Description</label>
                            <textarea class="form-control" id="description" name="description" rows="4" required><%= locals.formData?.description || '' %></textarea>
                        </div>
                        
                        <div class="mb-3">
                            <label for="sizes" class="form-label">Sizes (comma-separated)</label>
                            <input type="text" class="form-control" id="sizes" name="sizes" placeholder="e.g. S, M, L, XL" value="<%= locals.formData?.sizes || '' %>">
                            <div class="form-text">Leave empty if not applicable</div>
                        </div>
                        
                        <div class="mb-3">
                            <label for="colors" class="form-label">Colors (comma-separated)</label>
                            <input type="text" class="form-control" id="colors" name="colors" placeholder="e.g. Red, Blue, Black" value="<%= locals.formData?.colors || '' %>">
                            <div class="form-text">Leave empty if not applicable</div>
                        </div>
                        
                        <div class="mb-3">
                            <label for="imageUrl" class="form-label">Image URL</label>
                            <input type="text" class="form-control" id="imageUrl" name="imageUrl" placeholder="e.g. /images/product.jpg" value="<%= locals.formData?.imageUrl || '' %>">
                            <div class="form-text">Leave empty to use default placeholder</div>
                        </div>
                        
                        <div class="mb-3 form-check">
                            <input type="checkbox" class="form-check-input" id="inStock" name="inStock" <%= locals.formData?.inStock ? 'checked' : '' %>>
                            <label class="form-check-label" for="inStock">In Stock</label>
                        </div>
                        
                        <div class="mb-4 form-check">
                            <input type="checkbox" class="form-check-input" id="featured" name="featured" <%= locals.formData?.featured ? 'checked' : '' %>>
                            <label class="form-check-label" for="featured">Featured Product</label>
                        </div>
                        
                        <div class="d-flex justify-content-between">
                            <a href="/admin" class="btn btn-outline-secondary">Cancel</a>
                            <button type="submit" class="btn btn-primary">Add Product</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
